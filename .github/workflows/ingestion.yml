name: Daily Ingestion

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: rp-rocket-mlb-data-writers
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: true

      - name: Configure GitHub Actions push access
        run: |
          git remote set-url origin \
            "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Nightly Savant refresh
        run: |
          # run even off-season; the script itself exits gracefully
          python scripts/nightly_savant_refresh.py

      - name: Refresh current-season retrosheet data
        run: |
          # update 2026 game logs, batting, pitching, and player registry
          python scripts/fetch_current_season.py

      - name: Refresh reference data (Guts!, park factors, Chadwick)
        run: |
          python scripts/fetch_reference_data.py

      - name: Run ingestion (season-aware)
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: |
          python - <<'PYTHON'
          from datetime import datetime
          import sys
          from src.ingestion.season import in_season

          if not in_season(datetime.utcnow()):
              print('Outside baseball season; skipping ingestion.\n' +
                    'Season months are Mar–Nov and year must be within config range.')
              sys.exit(0)

          # Morning pull removed — daily picks pipeline handles odds fetch.
          # afternoon_update is omitted to stay within the 500 req/month quota.
          from src.ingestion.scheduler import overnight_results

          overnight_results()
          PYTHON
      - name: Pre-compute aggregated datasets
        run: python scripts/precompute_data.py

      - name: Commit precomputed data and current-season supplements
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name  "github-actions[bot]"
          git add data_files/processed/
          git add data_files/retrosheet/*_current.parquet 2>/dev/null || true

          if git diff --staged --quiet; then
            echo "No precomputed data changes to commit."
            exit 0
          fi

          git commit -m "chore: update precomputed data and season supplements [skip ci]"

          for attempt in 1 2 3; do
            if git push; then
              exit 0
            fi
            echo "Push raced with another writer; syncing with origin before retry ${attempt}/3..."
            git fetch origin "${GITHUB_REF_NAME}"
            git reset --soft "origin/${GITHUB_REF_NAME}"
            git commit -m "chore: update precomputed data and season supplements [skip ci]"
          done
          echo "Unable to push generated artifact after 3 attempts."
          exit 1
        continue-on-error: true

      - name: Train models (season-aware)
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: |
          python - <<'PYTHON'
          from datetime import datetime
          import sys
          from src.ingestion.season import in_season

          if not in_season(datetime.utcnow()):
              print('Outside baseball season; skipping model training.')
              sys.exit(0)

          # use the helper script
          from scripts.train_models import main as train_main
          train_main()
          PYTHON

      - name: Run daily picks pipeline (season-aware)
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: |
          python - <<'PYTHON'
          from datetime import datetime
          import os
          import sys
          from src.ingestion.season import in_season

          if not in_season(datetime.utcnow()):
              print('Outside baseball season; skipping picks pipeline.')
              sys.exit(0)

          if not os.environ.get("ODDS_API_KEY"):
              print("Skipping daily picks: ODDS_API_KEY is not configured.")
              sys.exit(0)

          from src.picks.daily_pipeline import run_daily_pipeline
          try:
              run_daily_pipeline()
          except Exception as exc:
              from src.picks.daily_pipeline import write_pipeline_status
              write_pipeline_status(
                  "pipeline_failed",
                  notes=f"{type(exc).__name__}: {exc}",
              )
              raise
          PYTHON

      - name: Export best bets for Sports Picks Grid
        if: always()
        run: python scripts/export_best_bets.py
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Validate dashboard export
        if: always()
        run: |
          python - <<'PY'
          import json
          from datetime import datetime, timezone
          from pathlib import Path

          path = Path("data_files/best_bets_today.json")
          payload = json.loads(path.read_text(encoding="utf-8"))
          meta = payload.get("meta", {})
          bets = payload.get("bets")
          assert meta.get("sport") == "MLB"
          assert meta.get("status") in {"ok", "no_picks", "off_season", "pipeline_pending", "pipeline_failed", "no_qualifying_picks", "no_games"}
          assert meta.get("tier_definition") == "edge-v1"
          assert isinstance(bets, list)
          datetime.fromisoformat(meta["generated_at"].replace("Z", "+00:00")).astimezone(timezone.utc)
          required = {"game_date", "game", "pick", "confidence", "edge", "odds", "tier"}
          for index, bet in enumerate(bets):
              missing = required - bet.keys()
              assert not missing, f"bets[{index}] missing {sorted(missing)}"
          print(f"Validated {len(bets)} MLB bets with status {meta['status']}")
          PY

      - name: Commit best_bets_today.json
        if: always()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name  "github-actions[bot]"
          git add data_files/best_bets_today.json

          if git diff --staged --quiet; then
            echo "No best_bets_today.json changes to commit."
            exit 0
          fi

          git commit -m "chore: update best_bets_today [skip ci]"

          for attempt in 1 2 3; do
            if git push; then
              exit 0
            fi
            echo "Push raced with another writer; syncing with origin before retry ${attempt}/3..."
            git fetch origin "${GITHUB_REF_NAME}"
            git reset --soft "origin/${GITHUB_REF_NAME}"
            git commit -m "chore: update best_bets_today [skip ci]"
          done
          echo "Unable to push generated artifact after 3 attempts."
          exit 1
